import java.util.*;

/** Creates pods of specified size from students in graph parameter*/
public class PodFormation {
    StudentGraph graph;

    /** Constructor for PodFormation from students in StudentGraph */
    public PodFormation(StudentGraph graph) {
        // Constructor
    }
/** Creates pods of size podSize from students in StudentGraph */
    public void formPods(int podSize) {
        // Method signature only
    }
}
